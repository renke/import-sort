{
  "name": "import-sort-style",
  "version": "5.0.0",
  "description": "Interfaces for import-sort styles.",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc -p src",
    "build:watch": "tsc -p src -w",
    "test": "mocha --require ts-node/register --recursive \"test/**/*.ts\"",
    "test:watch": "mocha -w --require ts-node/register --recursive \"test/**/*.ts\""
  },
  "author": "Renke Grunwald <renke@renke.org> (https://github.com/renke)",
  "repository": "renke/import-sort",
  "license": "ISC",
  "files": [
    "lib"
  ],
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.3",
    "@types/node": "^10.3.5",
    "chai": "^4.0.2",
    "import-sort-parser": "^5.0.0",
    "mocha": "^5.2.0",
    "ts-node": "^8.0.2",
    "tslint": "^5.10.0",
    "typescript": "^3.2.4"
  }
}
