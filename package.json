{
  "name": "import-sort",
  "version": "0.3.0",
  "description": "Sort ES6 imports.",
  "main": "lib",
  "bin": {
    "import-sort": "./lib/cli.js"
  },
  "scripts": {
    "prepublish": "npm run compile",
    "compile": "babel src --out-dir lib",
    "auto-compile": "babel src --out-dir lib --watch",
    "test": "mocha --compilers js:babel/register",
    "auto-test": "mocha --compilers js:babel/register --watch"
  },
  "author": "Renke Grunwald <renke@renke.org> (https://github.com/renke)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/renke/import-sort.git"
  },
  "devDependencies": {
    "chai": "^3.2.0",
    "mocha": "^2.3.2"
  },
  "dependencies": {
    "babel": "^5.8.23"
  }
}
